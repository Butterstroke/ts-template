export default interface environment {
    NODE_ENV: string,
    API_PORT: number,
    API_ADDRESS: string,
    DB_USER: string,
    DB_PASSWORD: string,
    TARGET_DB: string,
    ASSET_DIR: string,
    PRODUCTION: boolean,
    DB_PORT: number
}